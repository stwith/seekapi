#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT_DIR"

echo "[seed-demo-data] placeholder"
echo "[seed-demo-data] future script should create a demo project, downstream api key, and provider credential record"
