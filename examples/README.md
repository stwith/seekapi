# Examples

This directory should contain canonical integration examples for clients.

Planned examples:

- canonical web search request
- canonical news search request
- canonical images search request
- authenticated request using a downstream API key

When a public request or response shape changes, update the matching example in this directory.
